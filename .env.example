PORT=5000
NODE_ENV=production
# MongoDB connection string (use MongoDB Atlas free cluster)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/gramavoice?retryWrites=true&w=majority
# Strong JWT secret - rotate in production
JWT_SECRET=change_this_to_a_strong_random_value
